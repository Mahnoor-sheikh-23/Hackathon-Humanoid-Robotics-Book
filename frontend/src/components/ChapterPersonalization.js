import React from 'react';

// A simple placeholder component since personalization is now handled in the navbar
const ChapterPersonalization = () => {
  return null; // No UI needed since personalization is handled via navbar button
};

export default ChapterPersonalization;